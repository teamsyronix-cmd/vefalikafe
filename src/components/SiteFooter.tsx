export default function SiteFooter() {
  return (
    <p className="py-6 text-center text-[11.5px] font-semibold text-muted">
      © {new Date().getFullYear()} Vefalı · Doğukan Cödel tarafından yapılmıştır
    </p>
  );
}
